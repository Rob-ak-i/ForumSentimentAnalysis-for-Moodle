First of all check for installed java, python3 and pymorphy2 library or install it via shell:
> sudo apt-get install java-11-openjdk
> sudo apt-get install python3
> sudo apt-get install python3-pip
For pymorphy2 installation see guide at readthedocs - https://pymorphy2.readthedocs.io/en/stable/user/guide.html#id2
or update python3 and pip packages, and enter command: 
> sudo pip install pymorphy2
for launching use command: 
> java -jar SentimentAnalyzer.jar
